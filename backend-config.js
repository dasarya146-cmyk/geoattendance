/* ================================================================
   backend-config.js — External Backend Configuration
   ================================================================
   Replace this URL with your Render deployment URL:
*/

window.BACKEND_URL = 'http://localhost:10000'; // Change to live URL when deployed
